extends layout

block content
  h1= title
   - var image_names=["m1.jpg","m2.jpg","m3.jpg","m4.jpg","m5.jpg"]
    - for(let i=0; i<3; i++) {
        - var img= Math.floor(Math.random()*5)
           img(src=`/images/${image_names[img]}` alt=`${img}` width='300px' height='300px')
    - }